diff --git a/node_modules/rc-virtual-list/es/List.d.ts b/node_modules/rc-virtual-list/es/List.d.ts
index c517366..1421b17 100644
--- a/node_modules/rc-virtual-list/es/List.d.ts
+++ b/node_modules/rc-virtual-list/es/List.d.ts
@@ -20,6 +20,10 @@ export interface ListProps<T> extends Omit<React.HTMLAttributes<any>, 'children'
     data: T[];
     height?: number;
     itemHeight?: number;
+    /** Treat every item as exactly itemHeight and calculate the visible range in O(1). */
+    itemHeightFixed?: boolean;
+    /** Resolve an exact height per item and calculate the visible range by binary search. */
+    itemHeightResolver?: (item: T, index: number) => number;
     /** If not match virtual scroll condition, Set List still use height of container. */
     fullHeight?: boolean;
     itemKey: React.Key | ((item: T) => React.Key);
diff --git a/node_modules/rc-virtual-list/es/List.js b/node_modules/rc-virtual-list/es/List.js
index 2926056..b1b9ee4 100644
--- a/node_modules/rc-virtual-list/es/List.js
+++ b/node_modules/rc-virtual-list/es/List.js
@@ -4,7 +4,7 @@ import _objectSpread from "@babel/runtime/helpers/esm/objectSpread2";
 import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
 import _slicedToArray from "@babel/runtime/helpers/esm/slicedToArray";
 import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
-var _excluded = ["prefixCls", "className", "height", "itemHeight", "fullHeight", "style", "data", "children", "itemKey", "virtual", "direction", "scrollWidth", "component", "onScroll", "onVirtualScroll", "onVisibleChange", "innerProps", "extraRender", "styles", "showScrollBar"];
+var _excluded = ["prefixCls", "className", "height", "itemHeight", "itemHeightFixed", "itemHeightResolver", "fullHeight", "style", "data", "children", "itemKey", "virtual", "direction", "scrollWidth", "component", "onScroll", "onVirtualScroll", "onVisibleChange", "innerProps", "extraRender", "styles", "showScrollBar"];
 import classNames from 'classnames';
 import ResizeObserver from 'rc-resize-observer';
 import { useEvent } from 'rc-util';
@@ -35,6 +35,8 @@ export function RawList(props, ref) {
     className = props.className,
     height = props.height,
     itemHeight = props.itemHeight,
+    itemHeightFixed = props.itemHeightFixed,
+    itemHeightResolver = props.itemHeightResolver,
     _props$fullHeight = props.fullHeight,
     fullHeight = _props$fullHeight === void 0 ? true : _props$fullHeight,
     style = props.style,
@@ -63,9 +65,23 @@ export function RawList(props, ref) {
     }
     return item === null || item === void 0 ? void 0 : item[itemKey];
   }, [itemKey]);
+  var mergedData = data || EMPTY_DATA;
+  var fixedHeightEnabled = !!(itemHeight && (itemHeightFixed || itemHeightResolver));
+  var fixedItemOffsets = React.useMemo(function () {
+    if (!itemHeightResolver || !itemHeight) {
+      return null;
+    }
+    var offsets = [0];
+    for (var i = 0; i < mergedData.length; i += 1) {
+      var resolvedHeight = itemHeightResolver ? itemHeightResolver(mergedData[i], i) : itemHeight;
+      var safeHeight = Number.isFinite(resolvedHeight) && resolvedHeight > 0 ? resolvedHeight : itemHeight;
+      offsets.push(offsets[i] + safeHeight);
+    }
+    return offsets;
+  }, [mergedData, itemHeight, itemHeightResolver]);
 
   // ================================ Height ================================
-  var _useHeights = useHeights(getKey, null, null),
+  var _useHeights = useHeights(getKey, null, null, fixedHeightEnabled),
     _useHeights2 = _slicedToArray(_useHeights, 4),
     setInstanceRef = _useHeights2[0],
     collectHeight = _useHeights2[1],
@@ -74,15 +90,19 @@ export function RawList(props, ref) {
 
   // ================================= MISC =================================
   var useVirtual = !!(virtual !== false && height && itemHeight);
-  var containerHeight = React.useMemo(function () {
+  var measuredContainerHeight = React.useMemo(function () {
     return Object.values(heights.maps).reduce(function (total, curr) {
       return total + curr;
     }, 0);
   }, [heights.id, heights.maps]);
+  var containerHeight = fixedItemOffsets ? fixedItemOffsets[fixedItemOffsets.length - 1] : itemHeightFixed ? itemHeight * mergedData.length : measuredContainerHeight;
   var inVirtual = useVirtual && data && (Math.max(itemHeight * data.length, containerHeight) > height || !!scrollWidth);
+  // Lists with known row geometry do not need wheel deltas replayed through
+  // React. Let the browser compositor own vertical scrolling while React
+  // updates the virtual window.
+  var nativeVerticalScroll = !!(inVirtual && (itemHeightFixed || !!fixedItemOffsets));
   var isRTL = direction === 'rtl';
   var mergedClassName = classNames(prefixCls, _defineProperty({}, "".concat(prefixCls, "-rtl"), isRTL), className);
-  var mergedData = data || EMPTY_DATA;
   var componentRef = useRef();
   var fillerInnerRef = useRef();
   var containerRef = useRef();
@@ -159,6 +179,76 @@ export function RawList(props, ref) {
           offset: undefined
         };
       }
+      if (itemHeightFixed && !fixedItemOffsets && itemHeight > 0) {
+        var fixedDataLen = mergedData.length;
+        var fixedScrollHeight = fixedDataLen * itemHeight;
+        if (fixedDataLen === 0) {
+          return {
+            scrollHeight: 0,
+            start: 0,
+            end: -1,
+            offset: 0
+          };
+        }
+        var fixedMaxScrollTop = Math.max(0, fixedScrollHeight - height);
+        var fixedOffsetTop = Math.max(0, Math.min(fixedMaxScrollTop, offsetTop));
+        // Keep at least one viewport mounted on each side so a large native
+        // scroll delta cannot expose the filler before React commits.
+        var fixedOverscanRows = Math.max(6, Math.ceil(height / itemHeight));
+        var fixedStartIndex = Math.min(fixedDataLen - 1, Math.max(0, Math.ceil(fixedOffsetTop / itemHeight) - fixedOverscanRows));
+        var fixedEndIndex = Math.min(fixedDataLen - 1, Math.floor((fixedOffsetTop + height) / itemHeight) + fixedOverscanRows);
+        return {
+          scrollHeight: fixedScrollHeight,
+          start: fixedStartIndex,
+          end: fixedEndIndex,
+          offset: fixedStartIndex * itemHeight
+        };
+      }
+      if (fixedItemOffsets && itemHeight > 0) {
+        var fixedDataLen = mergedData.length;
+        var fixedScrollHeight = fixedItemOffsets[fixedDataLen];
+        if (fixedDataLen === 0) {
+          return {
+            scrollHeight: 0,
+            start: 0,
+            end: -1,
+            offset: 0
+          };
+        }
+        var fixedMaxScrollTop = Math.max(0, fixedScrollHeight - height);
+        var fixedOffsetTop = Math.max(0, Math.min(fixedMaxScrollTop, offsetTop));
+        var startLow = 0;
+        var startHigh = fixedDataLen - 1;
+        while (startLow < startHigh) {
+          var startMid = startLow + startHigh >> 1;
+          if (fixedItemOffsets[startMid + 1] >= fixedOffsetTop) {
+            startHigh = startMid;
+          } else {
+            startLow = startMid + 1;
+          }
+        }
+        // Keep six rows around the viewport so native scrolling cannot expose
+        // an empty strip before the next virtual-window commit.
+        var fixedStartIndex = Math.max(0, startLow - 6);
+        var fixedViewportBottom = fixedOffsetTop + height;
+        var endLow = fixedStartIndex;
+        var endHigh = fixedDataLen;
+        while (endLow < endHigh) {
+          var endMid = endLow + endHigh >> 1;
+          if (fixedItemOffsets[endMid + 1] > fixedViewportBottom) {
+            endHigh = endMid;
+          } else {
+            endLow = endMid + 1;
+          }
+        }
+        var fixedEndIndex = Math.min(fixedDataLen - 1, endLow + 6);
+        return {
+          scrollHeight: fixedScrollHeight,
+          start: fixedStartIndex,
+          end: fixedEndIndex,
+          offset: fixedItemOffsets[fixedStartIndex]
+        };
+      }
       var itemTop = 0;
       var startIndex;
       var startOffset;
@@ -201,7 +291,7 @@ export function RawList(props, ref) {
         end: endIndex,
         offset: startOffset
       };
-    }, [inVirtual, useVirtual, offsetTop, mergedData, heightUpdatedMark, height]),
+    }, [inVirtual, useVirtual, offsetTop, mergedData, heightUpdatedMark, height, fixedItemOffsets, itemHeight, itemHeightFixed]),
     scrollHeight = _React$useMemo.scrollHeight,
     start = _React$useMemo.start,
     end = _React$useMemo.end,
@@ -213,6 +303,10 @@ export function RawList(props, ref) {
   // Which will make scroll jump.
   // Let's sync scroll top to avoid jump
   React.useLayoutEffect(function () {
+    if (fixedHeightEnabled) {
+      heights.resetRecord();
+      return;
+    }
     var changedRecord = heights.getRecord();
     if (changedRecord.size === 1) {
       var recordKey = Array.from(changedRecord.keys())[0];
@@ -232,7 +326,7 @@ export function RawList(props, ref) {
       }
     }
     heights.resetRecord();
-  }, [scrollHeight]);
+  }, [scrollHeight, fixedHeightEnabled, heightUpdatedMark]);
 
   // ================================= Size =================================
   var _React$useState = React.useState({
@@ -388,6 +482,9 @@ export function RawList(props, ref) {
       }
     }
     var componentEle = componentRef.current;
+    if (nativeVerticalScroll) {
+      return undefined;
+    }
     componentEle.addEventListener('wheel', onRawWheel, {
       passive: false
     });
@@ -402,7 +499,7 @@ export function RawList(props, ref) {
       componentEle.removeEventListener('DOMMouseScroll', onFireFoxScroll);
       componentEle.removeEventListener('MozMousePixelScroll', onMozMousePixelScroll);
     };
-  }, [useVirtual, isScrollAtTop, isScrollAtBottom]);
+  }, [useVirtual, isScrollAtTop, isScrollAtBottom, nativeVerticalScroll]);
 
   // Sync scroll left
   useLayoutEffect(function () {
@@ -423,7 +520,7 @@ export function RawList(props, ref) {
   };
   var _scrollTo = useScrollTo(componentRef, mergedData, heights, itemHeight, getKey, function () {
     return collectHeight(true);
-  }, syncScrollTop, delayHideScrollBar);
+  }, syncScrollTop, delayHideScrollBar, fixedItemOffsets, itemHeightFixed);
   React.useImperativeHandle(ref, function () {
     return {
       nativeElement: containerRef.current,
@@ -457,7 +554,7 @@ export function RawList(props, ref) {
   }, [start, end, mergedData]);
 
   // ================================ Extra =================================
-  var getSize = useGetSize(mergedData, getKey, heights, itemHeight);
+  var getSize = useGetSize(mergedData, getKey, heights, itemHeight, fixedItemOffsets, itemHeightFixed);
   var extraContent = extraRender === null || extraRender === void 0 ? void 0 : extraRender({
     start: start,
     end: end,
@@ -474,7 +571,7 @@ export function RawList(props, ref) {
   if (height) {
     componentStyle = _objectSpread(_defineProperty({}, fullHeight ? 'height' : 'maxHeight', height), ScrollStyle);
     if (useVirtual) {
-      componentStyle.overflowY = 'hidden';
+      componentStyle.overflowY = nativeVerticalScroll ? 'auto' : 'hidden';
       if (scrollWidth) {
         componentStyle.overflowX = 'hidden';
       }
@@ -512,7 +609,7 @@ export function RawList(props, ref) {
     innerProps: innerProps,
     rtl: isRTL,
     extra: extraContent
-  }, listChildren))), inVirtual && scrollHeight > height && /*#__PURE__*/React.createElement(ScrollBar, {
+  }, listChildren))), inVirtual && scrollHeight > height && !nativeVerticalScroll && /*#__PURE__*/React.createElement(ScrollBar, {
     ref: verticalScrollBarRef,
     prefixCls: prefixCls,
     scrollOffset: offsetTop,
@@ -545,4 +642,4 @@ export function RawList(props, ref) {
 }
 var List = /*#__PURE__*/React.forwardRef(RawList);
 List.displayName = 'List';
-export default List;
\ No newline at end of file
+export default List;
diff --git a/node_modules/rc-virtual-list/es/hooks/useGetSize.d.ts b/node_modules/rc-virtual-list/es/hooks/useGetSize.d.ts
index adc0bea..e35a75d 100644
--- a/node_modules/rc-virtual-list/es/hooks/useGetSize.d.ts
+++ b/node_modules/rc-virtual-list/es/hooks/useGetSize.d.ts
@@ -4,4 +4,4 @@ import type { GetKey, GetSize } from '../interface';
  * Size info need loop query for the `heights` which will has the perf issue.
  * Let cache result for each render phase.
  */
-export declare function useGetSize<T>(mergedData: T[], getKey: GetKey<T>, heights: CacheMap, itemHeight: number): GetSize;
+export declare function useGetSize<T>(mergedData: T[], getKey: GetKey<T>, heights: CacheMap, itemHeight: number, fixedItemOffsets?: number[] | null, itemHeightFixed?: boolean): GetSize;
diff --git a/node_modules/rc-virtual-list/es/hooks/useGetSize.js b/node_modules/rc-virtual-list/es/hooks/useGetSize.js
index 9114f71..2b29d78 100644
--- a/node_modules/rc-virtual-list/es/hooks/useGetSize.js
+++ b/node_modules/rc-virtual-list/es/hooks/useGetSize.js
@@ -5,10 +5,10 @@ import * as React from 'react';
  * Size info need loop query for the `heights` which will has the perf issue.
  * Let cache result for each render phase.
  */
-export function useGetSize(mergedData, getKey, heights, itemHeight) {
+export function useGetSize(mergedData, getKey, heights, itemHeight, fixedItemOffsets, itemHeightFixed) {
   var _React$useMemo = React.useMemo(function () {
       return [new Map(), []];
-    }, [mergedData, heights.id, itemHeight]),
+    }, [mergedData, heights.id, itemHeight, fixedItemOffsets, itemHeightFixed]),
     _React$useMemo2 = _slicedToArray(_React$useMemo, 2),
     key2Index = _React$useMemo2[0],
     bottomList = _React$useMemo2[1];
@@ -26,8 +26,14 @@ export function useGetSize(mergedData, getKey, heights, itemHeight) {
         var item = mergedData[i];
         var key = getKey(item);
         key2Index.set(key, i);
-        var cacheHeight = (_heights$get = heights.get(key)) !== null && _heights$get !== void 0 ? _heights$get : itemHeight;
-        bottomList[i] = (bottomList[i - 1] || 0) + cacheHeight;
+        if (fixedItemOffsets) {
+          bottomList[i] = fixedItemOffsets[i + 1];
+        } else if (itemHeightFixed) {
+          bottomList[i] = (i + 1) * itemHeight;
+        } else {
+          var cacheHeight = (_heights$get = heights.get(key)) !== null && _heights$get !== void 0 ? _heights$get : itemHeight;
+          bottomList[i] = (bottomList[i - 1] || 0) + cacheHeight;
+        }
         if (key === startKey) {
           startIndex = i;
         }
@@ -45,4 +51,4 @@ export function useGetSize(mergedData, getKey, heights, itemHeight) {
     };
   };
   return getSize;
-}
\ No newline at end of file
+}
diff --git a/node_modules/rc-virtual-list/es/hooks/useHeights.d.ts b/node_modules/rc-virtual-list/es/hooks/useHeights.d.ts
index 481f822..be7d41a 100644
--- a/node_modules/rc-virtual-list/es/hooks/useHeights.d.ts
+++ b/node_modules/rc-virtual-list/es/hooks/useHeights.d.ts
@@ -1,6 +1,6 @@
 import type { GetKey } from '../interface';
 import CacheMap from '../utils/CacheMap';
-export default function useHeights<T>(getKey: GetKey<T>, onItemAdd?: (item: T) => void, onItemRemove?: (item: T) => void): [
+export default function useHeights<T>(getKey: GetKey<T>, onItemAdd?: (item: T) => void, onItemRemove?: (item: T) => void, disabled?: boolean): [
     setInstanceRef: (item: T, instance: HTMLElement) => void,
     collectHeight: (sync?: boolean) => void,
     cacheMap: CacheMap,
diff --git a/node_modules/rc-virtual-list/es/hooks/useHeights.js b/node_modules/rc-virtual-list/es/hooks/useHeights.js
index 0323aae..5334215 100644
--- a/node_modules/rc-virtual-list/es/hooks/useHeights.js
+++ b/node_modules/rc-virtual-list/es/hooks/useHeights.js
@@ -6,7 +6,7 @@ function parseNumber(value) {
   var num = parseFloat(value);
   return isNaN(num) ? 0 : num;
 }
-export default function useHeights(getKey, onItemAdd, onItemRemove) {
+export default function useHeights(getKey, onItemAdd, onItemRemove, disabled) {
   var _React$useState = React.useState(0),
     _React$useState2 = _slicedToArray(_React$useState, 2),
     updatedMark = _React$useState2[0],
@@ -14,12 +14,17 @@ export default function useHeights(getKey, onItemAdd, onItemRemove) {
   var instanceRef = useRef(new Map());
   var heightsRef = useRef(new CacheMap());
   var promiseIdRef = useRef(0);
+  var disabledRef = useRef(disabled);
+  disabledRef.current = disabled;
   function cancelRaf() {
     promiseIdRef.current += 1;
   }
   function collectHeight() {
     var sync = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
     cancelRaf();
+    if (disabledRef.current) {
+      return;
+    }
     var doCollect = function doCollect() {
       var changed = false;
       instanceRef.current.forEach(function (element, key) {
@@ -58,6 +63,9 @@ export default function useHeights(getKey, onItemAdd, onItemRemove) {
     }
   }
   function setInstanceRef(item, instance) {
+    if (disabledRef.current) {
+      return;
+    }
     var key = getKey(item);
     var origin = instanceRef.current.get(key);
     if (instance) {
@@ -79,5 +87,10 @@ export default function useHeights(getKey, onItemAdd, onItemRemove) {
   useEffect(function () {
     return cancelRaf;
   }, []);
+  useEffect(function () {
+    if (!disabled) {
+      collectHeight(true);
+    }
+  }, [disabled]);
   return [setInstanceRef, collectHeight, heightsRef.current, updatedMark];
-}
\ No newline at end of file
+}
diff --git a/node_modules/rc-virtual-list/es/hooks/useScrollTo.d.ts b/node_modules/rc-virtual-list/es/hooks/useScrollTo.d.ts
index d3352d1..27f0441 100644
--- a/node_modules/rc-virtual-list/es/hooks/useScrollTo.d.ts
+++ b/node_modules/rc-virtual-list/es/hooks/useScrollTo.d.ts
@@ -15,4 +15,4 @@ export type ScrollTarget = {
     align?: ScrollAlign;
     offset?: number;
 };
-export default function useScrollTo<T>(containerRef: React.RefObject<HTMLDivElement>, data: T[], heights: CacheMap, itemHeight: number, getKey: GetKey<T>, collectHeight: () => void, syncScrollTop: (newTop: number) => void, triggerFlash: () => void): (arg: number | ScrollTarget) => void;
+export default function useScrollTo<T>(containerRef: React.RefObject<HTMLDivElement>, data: T[], heights: CacheMap, itemHeight: number, getKey: GetKey<T>, collectHeight: () => void, syncScrollTop: (newTop: number) => void, triggerFlash: () => void, fixedItemOffsets?: number[] | null, itemHeightFixed?: boolean): (arg: number | ScrollTarget) => void;
diff --git a/node_modules/rc-virtual-list/es/hooks/useScrollTo.js b/node_modules/rc-virtual-list/es/hooks/useScrollTo.js
index e7958f9..c35bdcb 100644
--- a/node_modules/rc-virtual-list/es/hooks/useScrollTo.js
+++ b/node_modules/rc-virtual-list/es/hooks/useScrollTo.js
@@ -7,7 +7,7 @@ import raf from "rc-util/es/raf";
 import useLayoutEffect from "rc-util/es/hooks/useLayoutEffect";
 import { warning } from 'rc-util';
 var MAX_TIMES = 10;
-export default function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHeight, syncScrollTop, triggerFlash) {
+export default function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHeight, syncScrollTop, triggerFlash, fixedItemOffsets, itemHeightFixed) {
   var scrollRef = React.useRef();
   var _React$useState = React.useState(null),
     _React$useState2 = _slicedToArray(_React$useState, 2),
@@ -24,7 +24,9 @@ export default function useScrollTo(containerRef, data, heights, itemHeight, get
         });
         return;
       }
-      collectHeight();
+      if (!fixedItemOffsets && !itemHeightFixed) {
+        collectHeight();
+      }
       var targetAlign = syncState.targetAlign,
         originAlign = syncState.originAlign,
         index = syncState.index,
@@ -39,30 +41,40 @@ export default function useScrollTo(containerRef, data, heights, itemHeight, get
         var mergedAlign = targetAlign || originAlign;
 
         // Get top & bottom
-        var stackTop = 0;
         var itemTop = 0;
         var itemBottom = 0;
         var maxLen = Math.min(data.length - 1, index);
-        for (var i = 0; i <= maxLen; i += 1) {
-          var key = getKey(data[i]);
-          itemTop = stackTop;
-          var cacheHeight = heights.get(key);
-          itemBottom = itemTop + (cacheHeight === undefined ? itemHeight : cacheHeight);
-          stackTop = itemBottom;
+        if (fixedItemOffsets) {
+          itemTop = fixedItemOffsets[maxLen] || 0;
+          itemBottom = fixedItemOffsets[maxLen + 1] || itemTop;
+        } else if (itemHeightFixed) {
+          itemTop = Math.max(0, maxLen) * itemHeight;
+          itemBottom = maxLen >= 0 ? itemTop + itemHeight : 0;
+        } else {
+          var stackTop = 0;
+          for (var i = 0; i <= maxLen; i += 1) {
+            var key = getKey(data[i]);
+            itemTop = stackTop;
+            var cacheHeight = heights.get(key);
+            itemBottom = itemTop + (cacheHeight === undefined ? itemHeight : cacheHeight);
+            stackTop = itemBottom;
+          }
         }
 
         // Check if need sync height (visible range has item not record height)
-        var leftHeight = mergedAlign === 'top' ? offset : height - offset;
-        for (var _i = maxLen; _i >= 0; _i -= 1) {
-          var _key = getKey(data[_i]);
-          var _cacheHeight = heights.get(_key);
-          if (_cacheHeight === undefined) {
-            needCollectHeight = true;
-            break;
-          }
-          leftHeight -= _cacheHeight;
-          if (leftHeight <= 0) {
-            break;
+        if (!fixedItemOffsets && !itemHeightFixed) {
+          var leftHeight = mergedAlign === 'top' ? offset : height - offset;
+          for (var _i = maxLen; _i >= 0; _i -= 1) {
+            var _key = getKey(data[_i]);
+            var _cacheHeight = heights.get(_key);
+            if (_cacheHeight === undefined) {
+              needCollectHeight = true;
+              break;
+            }
+            leftHeight -= _cacheHeight;
+            if (leftHeight <= 0) {
+              break;
+            }
           }
         }
 
@@ -90,7 +102,7 @@ export default function useScrollTo(containerRef, data, heights, itemHeight, get
         }
 
         // One more time for sync
-        if (targetTop !== syncState.lastTop) {
+        if (!fixedItemOffsets && !itemHeightFixed && targetTop !== syncState.lastTop) {
           needCollectHeight = true;
         }
       }
@@ -140,4 +152,4 @@ export default function useScrollTo(containerRef, data, heights, itemHeight, get
       });
     }
   };
-}
\ No newline at end of file
+}
diff --git a/node_modules/rc-virtual-list/lib/List.d.ts b/node_modules/rc-virtual-list/lib/List.d.ts
index c517366..1421b17 100644
--- a/node_modules/rc-virtual-list/lib/List.d.ts
+++ b/node_modules/rc-virtual-list/lib/List.d.ts
@@ -20,6 +20,10 @@ export interface ListProps<T> extends Omit<React.HTMLAttributes<any>, 'children'
     data: T[];
     height?: number;
     itemHeight?: number;
+    /** Treat every item as exactly itemHeight and calculate the visible range in O(1). */
+    itemHeightFixed?: boolean;
+    /** Resolve an exact height per item and calculate the visible range by binary search. */
+    itemHeightResolver?: (item: T, index: number) => number;
     /** If not match virtual scroll condition, Set List still use height of container. */
     fullHeight?: boolean;
     itemKey: React.Key | ((item: T) => React.Key);
diff --git a/node_modules/rc-virtual-list/lib/List.js b/node_modules/rc-virtual-list/lib/List.js
index f0cb64d..33408d7 100644
--- a/node_modules/rc-virtual-list/lib/List.js
+++ b/node_modules/rc-virtual-list/lib/List.js
@@ -32,7 +32,7 @@ var _useScrollDrag = _interopRequireDefault(require("./hooks/useScrollDrag"));
 var _useScrollTo = _interopRequireDefault(require("./hooks/useScrollTo"));
 var _ScrollBar = _interopRequireDefault(require("./ScrollBar"));
 var _scrollbarUtil = require("./utils/scrollbarUtil");
-var _excluded = ["prefixCls", "className", "height", "itemHeight", "fullHeight", "style", "data", "children", "itemKey", "virtual", "direction", "scrollWidth", "component", "onScroll", "onVirtualScroll", "onVisibleChange", "innerProps", "extraRender", "styles", "showScrollBar"];
+var _excluded = ["prefixCls", "className", "height", "itemHeight", "itemHeightFixed", "itemHeightResolver", "fullHeight", "style", "data", "children", "itemKey", "virtual", "direction", "scrollWidth", "component", "onScroll", "onVirtualScroll", "onVisibleChange", "innerProps", "extraRender", "styles", "showScrollBar"];
 var EMPTY_DATA = [];
 var ScrollStyle = {
   overflowY: 'auto',
@@ -44,6 +44,8 @@ function RawList(props, ref) {
     className = props.className,
     height = props.height,
     itemHeight = props.itemHeight,
+    itemHeightFixed = props.itemHeightFixed,
+    itemHeightResolver = props.itemHeightResolver,
     _props$fullHeight = props.fullHeight,
     fullHeight = _props$fullHeight === void 0 ? true : _props$fullHeight,
     style = props.style,
@@ -72,9 +74,23 @@ function RawList(props, ref) {
     }
     return item === null || item === void 0 ? void 0 : item[itemKey];
   }, [itemKey]);
+  var mergedData = data || EMPTY_DATA;
+  var fixedHeightEnabled = !!(itemHeight && (itemHeightFixed || itemHeightResolver));
+  var fixedItemOffsets = React.useMemo(function () {
+    if (!itemHeightResolver || !itemHeight) {
+      return null;
+    }
+    var offsets = [0];
+    for (var i = 0; i < mergedData.length; i += 1) {
+      var resolvedHeight = itemHeightResolver ? itemHeightResolver(mergedData[i], i) : itemHeight;
+      var safeHeight = Number.isFinite(resolvedHeight) && resolvedHeight > 0 ? resolvedHeight : itemHeight;
+      offsets.push(offsets[i] + safeHeight);
+    }
+    return offsets;
+  }, [mergedData, itemHeight, itemHeightResolver]);
 
   // ================================ Height ================================
-  var _useHeights = (0, _useHeights3.default)(getKey, null, null),
+  var _useHeights = (0, _useHeights3.default)(getKey, null, null, fixedHeightEnabled),
     _useHeights2 = (0, _slicedToArray2.default)(_useHeights, 4),
     setInstanceRef = _useHeights2[0],
     collectHeight = _useHeights2[1],
@@ -83,15 +99,19 @@ function RawList(props, ref) {
 
   // ================================= MISC =================================
   var useVirtual = !!(virtual !== false && height && itemHeight);
-  var containerHeight = React.useMemo(function () {
+  var measuredContainerHeight = React.useMemo(function () {
     return Object.values(heights.maps).reduce(function (total, curr) {
       return total + curr;
     }, 0);
   }, [heights.id, heights.maps]);
+  var containerHeight = fixedItemOffsets ? fixedItemOffsets[fixedItemOffsets.length - 1] : itemHeightFixed ? itemHeight * mergedData.length : measuredContainerHeight;
   var inVirtual = useVirtual && data && (Math.max(itemHeight * data.length, containerHeight) > height || !!scrollWidth);
+  // Lists with known row geometry do not need wheel deltas replayed through
+  // React. Let the browser compositor own vertical scrolling while React
+  // updates the virtual window.
+  var nativeVerticalScroll = !!(inVirtual && (itemHeightFixed || !!fixedItemOffsets));
   var isRTL = direction === 'rtl';
   var mergedClassName = (0, _classnames.default)(prefixCls, (0, _defineProperty2.default)({}, "".concat(prefixCls, "-rtl"), isRTL), className);
-  var mergedData = data || EMPTY_DATA;
   var componentRef = (0, _react.useRef)();
   var fillerInnerRef = (0, _react.useRef)();
   var containerRef = (0, _react.useRef)();
@@ -168,6 +188,76 @@ function RawList(props, ref) {
           offset: undefined
         };
       }
+      if (itemHeightFixed && !fixedItemOffsets && itemHeight > 0) {
+        var fixedDataLen = mergedData.length;
+        var fixedScrollHeight = fixedDataLen * itemHeight;
+        if (fixedDataLen === 0) {
+          return {
+            scrollHeight: 0,
+            start: 0,
+            end: -1,
+            offset: 0
+          };
+        }
+        var fixedMaxScrollTop = Math.max(0, fixedScrollHeight - height);
+        var fixedOffsetTop = Math.max(0, Math.min(fixedMaxScrollTop, offsetTop));
+        // Keep at least one viewport mounted on each side so a large native
+        // scroll delta cannot expose the filler before React commits.
+        var fixedOverscanRows = Math.max(6, Math.ceil(height / itemHeight));
+        var fixedStartIndex = Math.min(fixedDataLen - 1, Math.max(0, Math.ceil(fixedOffsetTop / itemHeight) - fixedOverscanRows));
+        var fixedEndIndex = Math.min(fixedDataLen - 1, Math.floor((fixedOffsetTop + height) / itemHeight) + fixedOverscanRows);
+        return {
+          scrollHeight: fixedScrollHeight,
+          start: fixedStartIndex,
+          end: fixedEndIndex,
+          offset: fixedStartIndex * itemHeight
+        };
+      }
+      if (fixedItemOffsets && itemHeight > 0) {
+        var fixedDataLen = mergedData.length;
+        var fixedScrollHeight = fixedItemOffsets[fixedDataLen];
+        if (fixedDataLen === 0) {
+          return {
+            scrollHeight: 0,
+            start: 0,
+            end: -1,
+            offset: 0
+          };
+        }
+        var fixedMaxScrollTop = Math.max(0, fixedScrollHeight - height);
+        var fixedOffsetTop = Math.max(0, Math.min(fixedMaxScrollTop, offsetTop));
+        var startLow = 0;
+        var startHigh = fixedDataLen - 1;
+        while (startLow < startHigh) {
+          var startMid = startLow + startHigh >> 1;
+          if (fixedItemOffsets[startMid + 1] >= fixedOffsetTop) {
+            startHigh = startMid;
+          } else {
+            startLow = startMid + 1;
+          }
+        }
+        // Keep six rows around the viewport so native scrolling cannot expose
+        // an empty strip before the next virtual-window commit.
+        var fixedStartIndex = Math.max(0, startLow - 6);
+        var fixedViewportBottom = fixedOffsetTop + height;
+        var endLow = fixedStartIndex;
+        var endHigh = fixedDataLen;
+        while (endLow < endHigh) {
+          var endMid = endLow + endHigh >> 1;
+          if (fixedItemOffsets[endMid + 1] > fixedViewportBottom) {
+            endHigh = endMid;
+          } else {
+            endLow = endMid + 1;
+          }
+        }
+        var fixedEndIndex = Math.min(fixedDataLen - 1, endLow + 6);
+        return {
+          scrollHeight: fixedScrollHeight,
+          start: fixedStartIndex,
+          end: fixedEndIndex,
+          offset: fixedItemOffsets[fixedStartIndex]
+        };
+      }
       var itemTop = 0;
       var startIndex;
       var startOffset;
@@ -210,7 +300,7 @@ function RawList(props, ref) {
         end: endIndex,
         offset: startOffset
       };
-    }, [inVirtual, useVirtual, offsetTop, mergedData, heightUpdatedMark, height]),
+    }, [inVirtual, useVirtual, offsetTop, mergedData, heightUpdatedMark, height, fixedItemOffsets, itemHeight, itemHeightFixed]),
     scrollHeight = _React$useMemo.scrollHeight,
     start = _React$useMemo.start,
     end = _React$useMemo.end,
@@ -222,6 +312,10 @@ function RawList(props, ref) {
   // Which will make scroll jump.
   // Let's sync scroll top to avoid jump
   React.useLayoutEffect(function () {
+    if (fixedHeightEnabled) {
+      heights.resetRecord();
+      return;
+    }
     var changedRecord = heights.getRecord();
     if (changedRecord.size === 1) {
       var recordKey = Array.from(changedRecord.keys())[0];
@@ -241,7 +335,7 @@ function RawList(props, ref) {
       }
     }
     heights.resetRecord();
-  }, [scrollHeight]);
+  }, [scrollHeight, fixedHeightEnabled, heightUpdatedMark]);
 
   // ================================= Size =================================
   var _React$useState = React.useState({
@@ -397,6 +491,9 @@ function RawList(props, ref) {
       }
     }
     var componentEle = componentRef.current;
+    if (nativeVerticalScroll) {
+      return undefined;
+    }
     componentEle.addEventListener('wheel', onRawWheel, {
       passive: false
     });
@@ -411,7 +508,7 @@ function RawList(props, ref) {
       componentEle.removeEventListener('DOMMouseScroll', onFireFoxScroll);
       componentEle.removeEventListener('MozMousePixelScroll', onMozMousePixelScroll);
     };
-  }, [useVirtual, isScrollAtTop, isScrollAtBottom]);
+  }, [useVirtual, isScrollAtTop, isScrollAtBottom, nativeVerticalScroll]);
 
   // Sync scroll left
   (0, _useLayoutEffect.default)(function () {
@@ -432,7 +529,7 @@ function RawList(props, ref) {
   };
   var _scrollTo = (0, _useScrollTo.default)(componentRef, mergedData, heights, itemHeight, getKey, function () {
     return collectHeight(true);
-  }, syncScrollTop, delayHideScrollBar);
+  }, syncScrollTop, delayHideScrollBar, fixedItemOffsets, itemHeightFixed);
   React.useImperativeHandle(ref, function () {
     return {
       nativeElement: containerRef.current,
@@ -466,7 +563,7 @@ function RawList(props, ref) {
   }, [start, end, mergedData]);
 
   // ================================ Extra =================================
-  var getSize = (0, _useGetSize.useGetSize)(mergedData, getKey, heights, itemHeight);
+  var getSize = (0, _useGetSize.useGetSize)(mergedData, getKey, heights, itemHeight, fixedItemOffsets, itemHeightFixed);
   var extraContent = extraRender === null || extraRender === void 0 ? void 0 : extraRender({
     start: start,
     end: end,
@@ -483,7 +580,7 @@ function RawList(props, ref) {
   if (height) {
     componentStyle = (0, _objectSpread3.default)((0, _defineProperty2.default)({}, fullHeight ? 'height' : 'maxHeight', height), ScrollStyle);
     if (useVirtual) {
-      componentStyle.overflowY = 'hidden';
+      componentStyle.overflowY = nativeVerticalScroll ? 'auto' : 'hidden';
       if (scrollWidth) {
         componentStyle.overflowX = 'hidden';
       }
@@ -521,7 +618,7 @@ function RawList(props, ref) {
     innerProps: innerProps,
     rtl: isRTL,
     extra: extraContent
-  }, listChildren))), inVirtual && scrollHeight > height && /*#__PURE__*/React.createElement(_ScrollBar.default, {
+  }, listChildren))), inVirtual && scrollHeight > height && !nativeVerticalScroll && /*#__PURE__*/React.createElement(_ScrollBar.default, {
     ref: verticalScrollBarRef,
     prefixCls: prefixCls,
     scrollOffset: offsetTop,
@@ -554,4 +651,4 @@ function RawList(props, ref) {
 }
 var List = /*#__PURE__*/React.forwardRef(RawList);
 List.displayName = 'List';
-var _default = exports.default = List;
\ No newline at end of file
+var _default = exports.default = List;
diff --git a/node_modules/rc-virtual-list/lib/hooks/useGetSize.d.ts b/node_modules/rc-virtual-list/lib/hooks/useGetSize.d.ts
index adc0bea..e35a75d 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useGetSize.d.ts
+++ b/node_modules/rc-virtual-list/lib/hooks/useGetSize.d.ts
@@ -4,4 +4,4 @@ import type { GetKey, GetSize } from '../interface';
  * Size info need loop query for the `heights` which will has the perf issue.
  * Let cache result for each render phase.
  */
-export declare function useGetSize<T>(mergedData: T[], getKey: GetKey<T>, heights: CacheMap, itemHeight: number): GetSize;
+export declare function useGetSize<T>(mergedData: T[], getKey: GetKey<T>, heights: CacheMap, itemHeight: number, fixedItemOffsets?: number[] | null, itemHeightFixed?: boolean): GetSize;
diff --git a/node_modules/rc-virtual-list/lib/hooks/useGetSize.js b/node_modules/rc-virtual-list/lib/hooks/useGetSize.js
index 39d9126..6dc97aa 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useGetSize.js
+++ b/node_modules/rc-virtual-list/lib/hooks/useGetSize.js
@@ -12,10 +12,10 @@ var React = _interopRequireWildcard(require("react"));
  * Size info need loop query for the `heights` which will has the perf issue.
  * Let cache result for each render phase.
  */
-function useGetSize(mergedData, getKey, heights, itemHeight) {
+function useGetSize(mergedData, getKey, heights, itemHeight, fixedItemOffsets, itemHeightFixed) {
   var _React$useMemo = React.useMemo(function () {
       return [new Map(), []];
-    }, [mergedData, heights.id, itemHeight]),
+    }, [mergedData, heights.id, itemHeight, fixedItemOffsets, itemHeightFixed]),
     _React$useMemo2 = (0, _slicedToArray2.default)(_React$useMemo, 2),
     key2Index = _React$useMemo2[0],
     bottomList = _React$useMemo2[1];
@@ -33,8 +33,14 @@ function useGetSize(mergedData, getKey, heights, itemHeight) {
         var item = mergedData[i];
         var key = getKey(item);
         key2Index.set(key, i);
-        var cacheHeight = (_heights$get = heights.get(key)) !== null && _heights$get !== void 0 ? _heights$get : itemHeight;
-        bottomList[i] = (bottomList[i - 1] || 0) + cacheHeight;
+        if (fixedItemOffsets) {
+          bottomList[i] = fixedItemOffsets[i + 1];
+        } else if (itemHeightFixed) {
+          bottomList[i] = (i + 1) * itemHeight;
+        } else {
+          var cacheHeight = (_heights$get = heights.get(key)) !== null && _heights$get !== void 0 ? _heights$get : itemHeight;
+          bottomList[i] = (bottomList[i - 1] || 0) + cacheHeight;
+        }
         if (key === startKey) {
           startIndex = i;
         }
@@ -52,4 +58,4 @@ function useGetSize(mergedData, getKey, heights, itemHeight) {
     };
   };
   return getSize;
-}
\ No newline at end of file
+}
diff --git a/node_modules/rc-virtual-list/lib/hooks/useHeights.d.ts b/node_modules/rc-virtual-list/lib/hooks/useHeights.d.ts
index 481f822..be7d41a 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useHeights.d.ts
+++ b/node_modules/rc-virtual-list/lib/hooks/useHeights.d.ts
@@ -1,6 +1,6 @@
 import type { GetKey } from '../interface';
 import CacheMap from '../utils/CacheMap';
-export default function useHeights<T>(getKey: GetKey<T>, onItemAdd?: (item: T) => void, onItemRemove?: (item: T) => void): [
+export default function useHeights<T>(getKey: GetKey<T>, onItemAdd?: (item: T) => void, onItemRemove?: (item: T) => void, disabled?: boolean): [
     setInstanceRef: (item: T, instance: HTMLElement) => void,
     collectHeight: (sync?: boolean) => void,
     cacheMap: CacheMap,
diff --git a/node_modules/rc-virtual-list/lib/hooks/useHeights.js b/node_modules/rc-virtual-list/lib/hooks/useHeights.js
index 471ffc6..b9530be 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useHeights.js
+++ b/node_modules/rc-virtual-list/lib/hooks/useHeights.js
@@ -14,7 +14,7 @@ function parseNumber(value) {
   var num = parseFloat(value);
   return isNaN(num) ? 0 : num;
 }
-function useHeights(getKey, onItemAdd, onItemRemove) {
+function useHeights(getKey, onItemAdd, onItemRemove, disabled) {
   var _React$useState = React.useState(0),
     _React$useState2 = (0, _slicedToArray2.default)(_React$useState, 2),
     updatedMark = _React$useState2[0],
@@ -22,12 +22,17 @@ function useHeights(getKey, onItemAdd, onItemRemove) {
   var instanceRef = (0, _react.useRef)(new Map());
   var heightsRef = (0, _react.useRef)(new _CacheMap.default());
   var promiseIdRef = (0, _react.useRef)(0);
+  var disabledRef = (0, _react.useRef)(disabled);
+  disabledRef.current = disabled;
   function cancelRaf() {
     promiseIdRef.current += 1;
   }
   function collectHeight() {
     var sync = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
     cancelRaf();
+    if (disabledRef.current) {
+      return;
+    }
     var doCollect = function doCollect() {
       var changed = false;
       instanceRef.current.forEach(function (element, key) {
@@ -66,6 +71,9 @@ function useHeights(getKey, onItemAdd, onItemRemove) {
     }
   }
   function setInstanceRef(item, instance) {
+    if (disabledRef.current) {
+      return;
+    }
     var key = getKey(item);
     var origin = instanceRef.current.get(key);
     if (instance) {
@@ -87,5 +95,10 @@ function useHeights(getKey, onItemAdd, onItemRemove) {
   (0, _react.useEffect)(function () {
     return cancelRaf;
   }, []);
+  (0, _react.useEffect)(function () {
+    if (!disabled) {
+      collectHeight(true);
+    }
+  }, [disabled]);
   return [setInstanceRef, collectHeight, heightsRef.current, updatedMark];
-}
\ No newline at end of file
+}
diff --git a/node_modules/rc-virtual-list/lib/hooks/useScrollTo.d.ts b/node_modules/rc-virtual-list/lib/hooks/useScrollTo.d.ts
index d3352d1..27f0441 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useScrollTo.d.ts
+++ b/node_modules/rc-virtual-list/lib/hooks/useScrollTo.d.ts
@@ -15,4 +15,4 @@ export type ScrollTarget = {
     align?: ScrollAlign;
     offset?: number;
 };
-export default function useScrollTo<T>(containerRef: React.RefObject<HTMLDivElement>, data: T[], heights: CacheMap, itemHeight: number, getKey: GetKey<T>, collectHeight: () => void, syncScrollTop: (newTop: number) => void, triggerFlash: () => void): (arg: number | ScrollTarget) => void;
+export default function useScrollTo<T>(containerRef: React.RefObject<HTMLDivElement>, data: T[], heights: CacheMap, itemHeight: number, getKey: GetKey<T>, collectHeight: () => void, syncScrollTop: (newTop: number) => void, triggerFlash: () => void, fixedItemOffsets?: number[] | null, itemHeightFixed?: boolean): (arg: number | ScrollTarget) => void;
diff --git a/node_modules/rc-virtual-list/lib/hooks/useScrollTo.js b/node_modules/rc-virtual-list/lib/hooks/useScrollTo.js
index 707bf35..1815cdb 100644
--- a/node_modules/rc-virtual-list/lib/hooks/useScrollTo.js
+++ b/node_modules/rc-virtual-list/lib/hooks/useScrollTo.js
@@ -16,7 +16,7 @@ var _rcUtil = require("rc-util");
 /* eslint-disable no-param-reassign */
 
 var MAX_TIMES = 10;
-function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHeight, syncScrollTop, triggerFlash) {
+function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHeight, syncScrollTop, triggerFlash, fixedItemOffsets, itemHeightFixed) {
   var scrollRef = React.useRef();
   var _React$useState = React.useState(null),
     _React$useState2 = (0, _slicedToArray2.default)(_React$useState, 2),
@@ -33,7 +33,9 @@ function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHei
         });
         return;
       }
-      collectHeight();
+      if (!fixedItemOffsets && !itemHeightFixed) {
+        collectHeight();
+      }
       var targetAlign = syncState.targetAlign,
         originAlign = syncState.originAlign,
         index = syncState.index,
@@ -48,30 +50,40 @@ function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHei
         var mergedAlign = targetAlign || originAlign;
 
         // Get top & bottom
-        var stackTop = 0;
         var itemTop = 0;
         var itemBottom = 0;
         var maxLen = Math.min(data.length - 1, index);
-        for (var i = 0; i <= maxLen; i += 1) {
-          var key = getKey(data[i]);
-          itemTop = stackTop;
-          var cacheHeight = heights.get(key);
-          itemBottom = itemTop + (cacheHeight === undefined ? itemHeight : cacheHeight);
-          stackTop = itemBottom;
+        if (fixedItemOffsets) {
+          itemTop = fixedItemOffsets[maxLen] || 0;
+          itemBottom = fixedItemOffsets[maxLen + 1] || itemTop;
+        } else if (itemHeightFixed) {
+          itemTop = Math.max(0, maxLen) * itemHeight;
+          itemBottom = maxLen >= 0 ? itemTop + itemHeight : 0;
+        } else {
+          var stackTop = 0;
+          for (var i = 0; i <= maxLen; i += 1) {
+            var key = getKey(data[i]);
+            itemTop = stackTop;
+            var cacheHeight = heights.get(key);
+            itemBottom = itemTop + (cacheHeight === undefined ? itemHeight : cacheHeight);
+            stackTop = itemBottom;
+          }
         }
 
         // Check if need sync height (visible range has item not record height)
-        var leftHeight = mergedAlign === 'top' ? offset : height - offset;
-        for (var _i = maxLen; _i >= 0; _i -= 1) {
-          var _key = getKey(data[_i]);
-          var _cacheHeight = heights.get(_key);
-          if (_cacheHeight === undefined) {
-            needCollectHeight = true;
-            break;
-          }
-          leftHeight -= _cacheHeight;
-          if (leftHeight <= 0) {
-            break;
+        if (!fixedItemOffsets && !itemHeightFixed) {
+          var leftHeight = mergedAlign === 'top' ? offset : height - offset;
+          for (var _i = maxLen; _i >= 0; _i -= 1) {
+            var _key = getKey(data[_i]);
+            var _cacheHeight = heights.get(_key);
+            if (_cacheHeight === undefined) {
+              needCollectHeight = true;
+              break;
+            }
+            leftHeight -= _cacheHeight;
+            if (leftHeight <= 0) {
+              break;
+            }
           }
         }
 
@@ -99,7 +111,7 @@ function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHei
         }
 
         // One more time for sync
-        if (targetTop !== syncState.lastTop) {
+        if (!fixedItemOffsets && !itemHeightFixed && targetTop !== syncState.lastTop) {
           needCollectHeight = true;
         }
       }
@@ -149,4 +161,4 @@ function useScrollTo(containerRef, data, heights, itemHeight, getKey, collectHei
       });
     }
   };
-}
\ No newline at end of file
+}
